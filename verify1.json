{
    "command": [
        "sh",
        "-c",
        "sqlite3 turktelekomtrigger.db 'CREATE TRIGGER audit_log AFTER INSERT ON company BGIN INSERT TO audit(emp_id,entry_date) VALUES(new.id,datetime('now')); END;' > /dev/null 2>&1 || exit 1"
    ]
}
